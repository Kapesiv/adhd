{
	"name": "iltavahti-backend",
	"private": true,
	"version": "0.1.0",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"db:init": "wrangler d1 execute iltavahti-db --file=./schema.sql",
		"db:init-local": "wrangler d1 execute iltavahti-db --local --file=./schema.sql",
		"vapid:generate": "node --experimental-vm-modules ./scripts/generate-vapid.mjs"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20250101.0",
		"typescript": "^5.6.0",
		"wrangler": "^4.5.0"
	}
}
